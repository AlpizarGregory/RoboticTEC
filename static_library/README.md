# RoboticTEC - Static Library

The library `lib_bridge.a` is used by the user to communicate with the Arduino using the custom driver.

Check the file `bridge.h` to know what methods can be called by the user.